# S16_LE for MSM8916 PCM playback and capture.
monitor.alsa.rules = [
	{
		matches = [
			{
				node.name = "~alsa_(input|output).platform-7702000.*"
			}
		]
		actions = {
			update-props = {
				audio.format = S16LE
			}
		}
	}
]
