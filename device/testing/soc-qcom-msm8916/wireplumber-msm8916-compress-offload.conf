# Keep ACP off the MSM8916 compress-offload device.
monitor.alsa.rules = [
	{
		matches = [
			{
				device.api = "alsa:compressed"
			}
		]
		actions = {
			update-props = {
				api.alsa.use-acp = false
			}
		}
	}
]
